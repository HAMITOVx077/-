<Window x:Class="МаршрутСборки.Views.Dialogs.ReworkComponentsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Замена компонентов (переделка)"
        Height="700" Width="680"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F8FAFC">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="White"
                BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                Padding="24,18">
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="18"
                           FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="Укажите компоненты для замены. Кладовщик выдаст только их."
                           FontSize="12" Foreground="#94A3B8" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Текущий состав + кнопка добавить замену -->
        <Grid Grid.Row="1" Margin="24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Текущий состав сборки:"
                       FontSize="13" FontWeight="SemiBold"
                       Foreground="#475569" Margin="0,0,0,8"/>

            <!-- Список текущих компонентов -->
            <Border Grid.Row="1" Background="White"
                    BorderBrush="#E2E8F0" BorderThickness="1"
                    CornerRadius="8" MaxHeight="160" Margin="0,0,0,16">
                <ListBox x:Name="CurrentComponentsList"
                         BorderThickness="0" Background="Transparent"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock FontSize="13" Padding="4,3">
                                <Run Text="{Binding Component.Name}" FontWeight="SemiBold"/>
                                <Run Text=" × " Foreground="#94A3B8"/>
                                <Run Text="{Binding Quantity}" Foreground="#3B82F6"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Добавить замену -->
            <TextBlock Grid.Row="2" Text="Замены компонентов:"
                       FontSize="13" FontWeight="SemiBold"
                       Foreground="#475569" Margin="0,0,0,8"/>

            <!-- Список запланированных замен -->
            <Border Grid.Row="3" Background="White"
                    BorderBrush="#E2E8F0" BorderThickness="1"
                    CornerRadius="8" MinHeight="100">
                <DataGrid x:Name="ReworkItemsGrid"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          BorderThickness="0" Background="Transparent"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="38" ColumnHeaderHeight="34"
                          CanUserResizeRows="False">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Снять" Width="*"
                                            Binding="{Binding OldName}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#EF4444"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Установить" Width="*"
                                            Binding="{Binding NewName}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#16A34A"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Кол-во" Width="60"
                                            Binding="{Binding Quantity}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#475569"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Форма добавления замены -->
            <Border Grid.Row="4" Background="White"
                    BorderBrush="#E2E8F0" BorderThickness="1"
                    CornerRadius="8" Padding="14,12" Margin="0,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Снять компонент" FontSize="11"
                                   Foreground="#94A3B8" Margin="0,0,0,4"/>
                        <ComboBox x:Name="OldComponentBox"
                                  FontSize="13" Padding="8,6"
                                  BorderBrush="#E2E8F0">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Component.Name}"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <TextBlock Grid.Column="1" Text="→"
                               FontSize="18" Foreground="#CBD5E1"
                               VerticalAlignment="Bottom" Margin="0,0,0,6"
                               HorizontalAlignment="Center"/>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Установить вместо" FontSize="11"
                                   Foreground="#94A3B8" Margin="0,0,0,4"/>
                        <ComboBox x:Name="NewComponentBox"
                                  FontSize="13" Padding="8,6"
                                  BorderBrush="#E2E8F0">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock TextTrimming="CharacterEllipsis">
                                        <Run Text="{Binding Name}"/>
                                        <Run Text=" (" Foreground="#94A3B8"/>
                                        <Run Text="{Binding StockBalance}" Foreground="#3B82F6"/>
                                        <Run Text=" шт)" Foreground="#94A3B8"/>
                                    </TextBlock>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Кол-во" FontSize="11"
                                   Foreground="#94A3B8" Margin="0,0,0,4"/>
                        <TextBox x:Name="QuantityBox" Text="1"
                                 FontSize="13" Padding="8,6"
                                 BorderBrush="#E2E8F0" BorderThickness="1"/>
                    </StackPanel>

                    <Button Grid.Column="6"
                            Content="+ Добавить"
                            Click="AddReworkItem_Click"
                            Style="{StaticResource PrimaryButton}"
                            Background="#3B82F6"
                            VerticalAlignment="Bottom"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Примечание -->
        <Border Grid.Row="2" Background="White"
                BorderBrush="#E2E8F0" BorderThickness="0,1,0,1"
                Padding="24,12">
            <StackPanel>
                <TextBlock Text="Причина переделки / примечание" FontSize="12"
                           Foreground="#64748B" Margin="0,0,0,6"/>
                <Border Background="#F8FAFC" BorderBrush="#E2E8F0"
                        BorderThickness="1" CornerRadius="6">
                    <TextBox x:Name="NotesBox" FontSize="13" Padding="10,8"
                             BorderThickness="0" Background="Transparent"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             Height="60"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Кнопки -->
        <Border Grid.Row="3" Background="White"
                BorderBrush="#E2E8F0" BorderThickness="0,0,0,0"
                Padding="24,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" IsCancel="True"
                        Style="{StaticResource PrimaryButton}"
                        Background="#F1F5F9" Foreground="#475569"
                        Margin="0,0,10,0"/>
                <Button Content="Сохранить и отправить на переделку"
                        Click="Save_Click"
                        Style="{StaticResource PrimaryButton}"
                        Background="#F59E0B"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
